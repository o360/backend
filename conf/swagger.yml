---
  swagger: "2.0"
  info:
    title: "Bitworks assessment API"
    version: "1.0"
  consumes:
    - application/json
  produces:
    - application/json
  host: "sop-ci.z1.netpoint-dc.com:9000/api/v1.0"
  securityDefinitions:
    api_key:
      type: apiKey
      name: Authorization
      description: JWT
      in: header

