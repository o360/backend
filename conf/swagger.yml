---
  swagger: "2.0"
  info:
    title: "Bitworks assessment API"
    version: "1.0"
  consumes:
    - application/json
  produces:
    - application/json
  host: "sop-ci.z1.netpoint-dc.com:9000"
  basePath: "/api/v1.0"
  schemes:
    - "http"
  securityDefinitions:
    apiKey:
      type: apiKey
      name: X-Auth-Token
      description: JWT
      in: header
  security:
    - apiKey: []
  responses:
    'BadRequest':
      description: Bad Request
      schema:
        $ref: '#/definitions/controllers.api.ErrorResponse'
    'Unauthorized':
      description: Unauthorized
      schema:
        $ref: '#/definitions/controllers.api.ErrorResponse'
    'Forbidden':
      description: Forbidden
      schema:
        $ref: '#/definitions/controllers.api.ErrorResponse'
    'NotFound':
      description: Not Found
      schema:
        $ref: '#/definitions/controllers.api.ErrorResponse'
    'InternalServerError':
      description: Internal Server Error
      schema:
        $ref: '#/definitions/controllers.api.ErrorResponse'
